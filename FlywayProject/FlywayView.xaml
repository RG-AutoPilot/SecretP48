<UserControl x:Class="FlywayProject.FlywayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" MaxWidth="600">
            <TextBlock Text="Flyway Deployment" FontSize="18" FontWeight="Bold" Foreground="#AA0000" Margin="0,0,0,20"/>
            <TextBlock Text="Flyway Directory" FontWeight="SemiBold" Foreground="#333"/>
            <TextBox x:Name="FlywayDirBox" Margin="0,5,0,15" Padding="5"/>
            <TextBlock Text="Source Environment" FontWeight="SemiBold" Foreground="#333"/>
            <TextBox x:Name="FlywaySourceBox" Margin="0,5,0,15" Padding="5"/>
            <TextBlock Text="Target Environment" FontWeight="SemiBold" Foreground="#333"/>
            <TextBox x:Name="FlywayTargetBox" Margin="0,5,0,15" Padding="5"/>
            <TextBlock Text="Additional Parameters (optional)" FontWeight="SemiBold" Foreground="#333"/>
            <TextBox x:Name="FlywayParamsBox" Margin="0,5,0,20" Padding="5"/>
            <Button Content="Deploy"
                    Width="120" Height="32" HorizontalAlignment="Left"
                    FontWeight="SemiBold" Foreground="White" Background="#AA0000"
                    BorderThickness="0" Cursor="Hand" Click="OnDeployClicked"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
